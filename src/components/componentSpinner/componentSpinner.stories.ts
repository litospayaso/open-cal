import { html } from 'lit';
import './index';

export default {
  title: 'Components/ComponentSpinner',
  component: 'component-spinner',
};

const Template = () => html`<component-spinner></component-spinner>`;

export const Default = Template.bind({});
