# Spinner

A simple, reusable loading spinner component.

## Usage
Use this component to indicate a loading state within the application. It centers itself and spins indefinitely.

```html
<component-spinner></component-spinner>
```
