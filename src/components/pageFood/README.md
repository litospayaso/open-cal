# PageFood

The main product details page.

## Features
- Fetches and displays detailed nutritional information for a product based on the `code` query parameter.
- Allows users to add/remove the product from favorites.
- Includes a reactive calculator to adjust nutrient values based on the quantity (grams).
- Handles loading and error states.

## URL Parameters
- `code`: The unique identifier of the product to display.
