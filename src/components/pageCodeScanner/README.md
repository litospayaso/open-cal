# Page Code Scanner

A page to scan barcodes using the device camera.

## Features

- Camera access request.
- Barcode detection using `BarcodeDetector` API.
- Redirects to `/food` with scanned code.
